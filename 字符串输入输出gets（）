#include<stdio.h>
#include<math.h>
#include<string.h>
int main()
{
   int n;
   char str[100][100];
   scanf("%d",&n);
   getchar();//吸收n之后的空格
   for(int i=0;i<n;i++)
   {
       gets(str[i]);//gets以\n为输入结束
   }
   for(int i=0;i<n;i++)
   {
       puts(str[i]);
   }
}
